# Singularity Landing

Generated public landing page for [Singularity](https://github.com/wisent-ai/singularity), the persistent autonomous digital being runtime.

Approved claims live in `singularity/landing.brief.json`; authored page structure lives in `singularity/landing.plan.json`. Regenerate with `@wisent-ai/landing-cli` rather than editing generated source.

```sh
npm ci
npm run build
```

Canonical origin: https://singularity.wisent.com
